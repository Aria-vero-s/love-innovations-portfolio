// Image with fallback component for vanilla JavaScript
// This is handled by the createImageWithFallback utility function

// No additional JavaScript needed as the functionality is in utils.js
window.ImageWithFallback = {
    create: createImageWithFallback
};